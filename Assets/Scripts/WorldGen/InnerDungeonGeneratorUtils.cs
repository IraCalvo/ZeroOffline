using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class InnerDungeonGeneratorUtils
{
    public static Vector2Int GetRandomSpawnPosition(HashSet<Vector2Int> floorPos)
    {
        //change hashset to array, choose random from array
        return Vector2Int.zero;
    }

    public static bool CheckObjectCollision(GameObject obj)
    {
        return false;
    }
}
